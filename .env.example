# Used by docker-compose.yml to provision the local TimescaleDB container.
# These are LOCAL DEV DEFAULTS ONLY. Copy to .env and change the password if
# this machine is ever exposed beyond localhost.
POSTGRES_USER=aegis
POSTGRES_PASSWORD=aegis_dev_password
POSTGRES_DB=aegis
POSTGRES_PORT=5432
